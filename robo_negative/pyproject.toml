[project]
name = "robo-negative"
version = "0.1.0"
description = "Targeted negative-data generation for RoboPRO — twin-pair desync library (PoC)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy", "h5py"]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
