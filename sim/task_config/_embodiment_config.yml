# Paths are relative to ASSETS_ROOT (repo-root assets/). Loaders resolve
# them with resolve_embodiment_path() so cwd does not matter.
# Only aloha-agilex is shipped; the other embodiments were pruned from the
# RoboPRO asset set and their paths are kept for reference only.
aloha-agilex:
  file_path: embodiments/aloha-agilex/

piper:
  file_path: embodiments/piper

franka-panda:
  file_path: embodiments/franka-panda/

ARX-X5:
  file_path: embodiments/ARX-X5

ur5-wsg:
  file_path: embodiments/ur5-wsg
