# Asset paths are relative to sim/ (the cwd for all eval/collect
# entry points). Assets live in the sibling benchmark/assets/ tree.
# Only aloha-agilex is shipped; the other embodiments were pruned from the
# RoboPRO asset set and their paths are kept for reference only.
aloha-agilex:
  file_path: "../benchmark/assets/embodiments/aloha-agilex/"

piper:
  file_path: "../benchmark/assets/embodiments/piper"

franka-panda:
  file_path: "../benchmark/assets/embodiments/franka-panda/"

ARX-X5:
  file_path: "../benchmark/assets/embodiments/ARX-X5"

ur5-wsg:
  file_path: "../benchmark/assets/embodiments/ur5-wsg"
